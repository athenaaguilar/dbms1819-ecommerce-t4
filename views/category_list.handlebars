<div class="container">
 <table class="table table-striped table-dark table-hover text-center">
    <thead>
      <tr>
        <th>CATEGORIES</th>
      </tr>
    </thead>
    <tbody>
      {{#each rows}}
      <tr>
        <td>{{this.name}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>